.starter-template
  h1 Makerlab 119
  -if @current_user
    = link_to "New Booking", new_booking_path(:location => "makerlab119"), class: "btn btn-blue"

  .calendar_makerlab119

javascript:
  setTimeout("jQuery('.calendar_makerlab119').fullCalendar( 'render' );",100);
  var initialize_calendar;
    initialize_calendar = function() {
      $('.calendar_makerlab119').fullCalendar({
          defaultView: 'agendaWeek',
          events: '/rooms/makerlab119.json',
          height: '100',
          minTime: '8:00:00',
          maxTime: '23:00:00',
          slotDuration: '00:15:00',
          allDaySlot: false,
          selectable: false,
          selectHelper: true,
          eventLimit: true,
        });
    };
    $(document).on('turbolinks:load', initialize_calendar);
